modules = ["nodejs-20", "web"]
run = "npm run build && HOST=0.0.0.0 PORT=3000 npm start"

[deployment]
build = "npm ci --include=dev && npm run build"
run = "HOST=0.0.0.0 PORT=3000 npm start"
deploymentTarget = "cloudrun"

[nix]
channel = "stable-25_05"

[[ports]]
localPort = 3000
externalPort = 80